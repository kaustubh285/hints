{
	"name": "@dev_desh/hints",
	"version": "0.1.0",
	"bin": {
		"hints": "build/cli.js"
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsx watch src/cli.ts",
		"link": "bun link",
		"resetup-local": "hints uninstall && hints status && bun unlink && bun run build && bun run link && hints install"
	},
	"dependencies": {
		"chalk": "^5.6.2",
		"commander": "^14.0.3",
		"picomatch": "^4.0.3",
		"shell-quote": "^1.8.3"
	},
	"devDependencies": {
		"@types/picomatch": "^4.0.2",
		"@types/shell-quote": "^1.7.5",
		"tsx": "^4.21.0",
		"typescript": "^5.9.3"
	}
}
